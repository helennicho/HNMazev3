using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* public class Coin: MonoBehaviour 
{
    //Create a reference to the CoinPoofPrefab
	public GameObject CoinPoof;
	public int coinCount=0;

	
	  my old code??
	  public static Coin Instance;

	void Start() {

	}

	void Awake() {
		if (Instance == null)
			Instance = this;

		CoinPoof.SetActive(false);
	}

	void OnDestroy() {
		if (Instance = this)
			Instance = null;
	}

		
	public void OnCoinClicked() {
		// Instantiate the CoinPoof Prefab where this coin is located

		// Make sure the poof animates vertically
		// Destroy this coin. Check the Unity documentation on how to use Destroy - see OnDestroy above
			CoinPoof.SetActive(true);
	}

	// not sure 9f this is needed (copied Udacity Carnival Carnival Scores script) and used CoinPoof.SetACtive(true)     
		//Object.Instantiate(CoinPoof, new Vector3(2, 4, 6), Quaternion.identity);
	

	//Create a reference to the CoinPoofPrefab public GameObject CoinPoof;

	public void OnCoinClicked() {
		// Instantiate the CoinPoof Prefab where this coin is located
		Instantiate(CoinPoof);
		CoinPoof.transform.position = gameObject.transform.position;
		// Make sure the poof animates vertically
		// Destroy this coin. Check the Unity documentation on how to use Destroy
		Destroy (gameObject, 0.5f);
		coinCount++;
	}
}
 */       
        
   




